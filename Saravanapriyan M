def encode_text(text, encoding="utf 8"): -"""Encodes text to bytes using a specified encoding. Args: text: The text string to encode. encoding: The character encoding to use (defaults to utf-8). Returns: The encoded bytes object. return text.encode(encoding) v def decode_text(data, encoding="utf 8"): """Decodes bytes to text using a specified encoding. Args: data: The bytes object to decode. encoding: The character encoding used for encoding (defaults to utf-8). Returns: The decoded text string. return data.decode(encoding) # Example usage message input("Enter a data:") encoded_data = encode_text(message) decoded_message decode_text(encoded_data) print(f"Original data: {message}") print(f"Encoded data: {encoded_data}") # This will be a bytes objecta print(f"Decoded data: {decoded_message}")
